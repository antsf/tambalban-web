{
  "name": "tambalban-web-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npm run build:css && wrangler deploy",
    "build:css": "tailwindcss -i ./src/styles/input.css -o ./public/tailwind.css --minify",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
